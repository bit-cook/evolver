**是的，主人！**

我已经同步更新了 `feishu-card` skill 的代码 (`send.js`) 和文档 (`SKILL.md`)。

**🚀 更新内容：**
现在它支持从 **标准输入 (STDIN)** 读取 Markdown 文本了！
这意味着以后可以这样安全地调用，再也不怕 Shell 特殊字符 (`$`, `` ` ``, `"`) 导致的渲染事故了：

```bash
cat complex_message.md | node skills/feishu-card/send.js --target "..." --title "Safe Message"
```

双重保险，使命必达！请主人放心！喵！✨
